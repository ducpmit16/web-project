@using WebSiteSushi.Models;
@model WebSiteSushi.Models.Menu_Detail

@{
    ViewBag.Title = "Delete";
    Layout = null;
}

<div class="modal-header panel-heading">
    <button type="button" class="close" data-dismiss="modal">&times;</button>
    <h4 class="modal-title">Delete Menu Detail Item</h4>
</div>
<div class="modal-body">
    @Html.HiddenFor(n => n.id_menu_detail)
    <p>Are You Sure ???</p>
</div>
<div class="modal-footer">
    <button type="button" class="btn btn-default" onclick="Erase(@ViewBag.id_menu_detail)" data-dismiss="modal">Submit</button>
    <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
</div>

<script>
    function Erase(id) {
        $.ajax({
            url: '@Url.Action("deleteMenuDetail", "MenuDetail")',
            type: 'post',
            data: {
                id_menu_detail: id
            },
            success: function (response) {
                if (response == 'True') {
                    $.ajax({
                        url: '@Url.Action("MenuDetailPartial", "MenuDetail")',
                        type: 'post',
                        success: function (data) {
                            $("#tbl-menu-detail").html("");
                            $("#tbl-menu-detail").html(data);

                            $('#delete-menu-detail').modal('hide');
                            $('body').removeClass('modal-open');
                            $('.modal-backdrop').remove();
                            $("body").css("padding", "0");
                        }
                    });
                }
            }
        });
    }
</script>